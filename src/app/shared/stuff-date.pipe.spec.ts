import { StuffDatePipe } from './stuff-date.pipe';

describe('StuffDatePipe', () => {
  it('create an instance', () => {
    const pipe = new StuffDatePipe();
    expect(pipe).toBeTruthy();
  });
});
